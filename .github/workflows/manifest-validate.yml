name: Plugin manifest validation (release gate)

# PLA-376 — release-time check that runs the host plugin-manifest
# validator against the built dist/manifest.js BEFORE a tarball can be
# packed or published. Mirrors the gate at
# packages/plugins/create-paperclip-plugin/src/index.ts (scaffold).

on:
  pull_request:
    paths:
      - 'src/manifest.ts'
      - 'src/globals.d.ts'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'esbuild.config.mjs'
      - 'rollup.config.mjs'
      - 'scripts/validate-manifest.mjs'
      - 'scripts/check-manifest-version.mjs'
      - 'tests/validate-manifest.spec.ts'
      - '.github/workflows/manifest-validate.yml'
  push:
    branches:
      - main
    paths:
      - 'src/manifest.ts'
      - 'src/globals.d.ts'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'esbuild.config.mjs'
      - 'rollup.config.mjs'
      - 'scripts/validate-manifest.mjs'
      - 'scripts/check-manifest-version.mjs'
      - 'tests/validate-manifest.spec.ts'
      - '.github/workflows/manifest-validate.yml'

jobs:
  validate-manifest:
    name: validate dist/manifest.js against pluginManifestV1Schema
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.4
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - name: Build manifest (and worker)
        run: pnpm run build
      - name: Run regression test (PLA-376)
        run: pnpm exec vitest run --config ./vitest.config.ts tests/validate-manifest.spec.ts
      - name: Validate built manifest
        run: pnpm run validate:manifest
      - name: Check manifest.version == package.json.version (PLA-526)
        run: pnpm run check:manifest-version
